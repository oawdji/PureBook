# ⚠️ 端口说明：
#   PostgreSQL 映射到 5433（避免与本地 PostgreSQL 5432 冲突）
#   Redis 映射到 6380（避免与本地 Redis 6379 冲突）
#   生产环境请恢复标准端口

services:
  nginx:
    image: nginx:1.25-alpine
    container_name: purebook-nginx
    ports:
      - "443:443"
    volumes:
      - ./docker/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./docker/certs:/etc/nginx/certs:ro
    depends_on:
      - app
    restart: unless-stopped
    networks:
      - purebook-network

  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: purebook-app
    expose:
      - "3000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - JWT_SECRET=${JWT_SECRET}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - purebook-network

  postgres:
    image: postgres:16-alpine
    container_name: purebook-postgres
    expose:
      - "5432"
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-purebook}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-purebook123}
      - POSTGRES_DB=${POSTGRES_DB:-purebook}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-purebook} -d ${POSTGRES_DB:-purebook}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - purebook-network

  redis:
    image: redis:7-alpine
    container_name: purebook-redis
    expose:
      - "6379"
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - purebook-network

volumes:
  pgdata:

networks:
  purebook-network:
    driver: bridge
